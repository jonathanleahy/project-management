FROM golang:1.23-alpine

RUN apk add --no-cache git gcc musl-dev

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

EXPOSE 8080

# Use go run for development instead of air
CMD ["go", "run", "cmd/server/main.go"]